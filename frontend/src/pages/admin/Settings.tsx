import { Settings as SettingsIcon } from 'lucide-react'
import { ComingSoon } from '@/components/shared/ComingSoon'

export default function Settings() {
  return <ComingSoon icon={SettingsIcon} title="System settings" />
}
